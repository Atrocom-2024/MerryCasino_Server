# node_modules 폴더는 NPM 패키지를 포함하므로 Git에 포함할 필요 없음
node_modules/

# 빌드된 TypeScript 파일들 (빌드 후 생성된 .js 파일)
build/

# 환경 변수 파일
.env

# 로그 파일
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 시스템 파일
.DS_Store
Thumbs.db

# TypeScript 캐시 및 설정 파일
*.tsbuildinfo

# IDE 설정 파일 (예: VSCode)
.vscode/
.idea/